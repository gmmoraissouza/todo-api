import 'package:flutter/material.dart';

const userStyle = TextStyle(fontSize: 20, fontWeight: FontWeight.bold);

const editStyle =
    TextStyle(fontSize: 20, fontWeight: FontWeight.bold, color: primaryColor);

const titleStyle =
    TextStyle(fontSize: 30, fontWeight: FontWeight.bold, color: primaryColor);
const italicStyle =
    TextStyle(fontStyle: FontStyle.italic, color: primaryColor, fontSize: 20);

const primaryColor = Colors.blueGrey;
const pagePadding = EdgeInsets.symmetric(horizontal: 40.0, vertical: 20.0);
